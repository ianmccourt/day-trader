Here is the current state of the account and the session.

{state}

Follow the system playbook this cycle: open risk first, then scan with
`get_quote` and `get_bars` before deciding. Either call `place_order` (up to
the per-cycle limit) with stop and take-profit on any new entry, or end your
turn with a short explanation of why you are not trading — after you have
looked.
