Here is the current state of the account and the session.

{state}

Decide what to do this cycle. Use the read-only tools if you need more than the
state above. Then either call `place_order` (up to the per-cycle limit), or end
your turn with a short explanation of why you are not trading.
